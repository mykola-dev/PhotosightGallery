<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ds.photosight"

    >

    <application
        android:name="ds.photosight.App"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        >

        <activity
            android:name=".view.MainActivity"
            android:label="@string/activity_name"
            android:theme="@style/SplashTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity android:name="ds.photosight_legacy.ui.PreferencesActivity" />-->

        <!--<activity
            android:name="ds.photosight_legacy.ui.GalleryActivity"
            android:label=".ui.GalleryActivity"
            android:theme="@style/Theme.MySherlockBlack"
            android:uiOptions="splitActionBarWhenNarrow"
            />-->

    </application>
</manifest>